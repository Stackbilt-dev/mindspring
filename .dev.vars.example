# Local development — copy to .dev.vars
# Used by `wrangler dev`, never deployed
#
# No secrets required — all services (Vectorize, AI, KV, R2, Queue)
# are accessed via Cloudflare bindings. Wrangler dev handles auth
# automatically when logged in.
